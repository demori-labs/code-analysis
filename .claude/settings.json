{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.cs|*.csx|*.csproj|*.props|*.targets|*.xml|*.xaml|*.slnx) dotnet csharpier format \"$f\";; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
